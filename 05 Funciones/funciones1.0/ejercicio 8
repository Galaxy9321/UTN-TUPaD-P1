#Funciones
def calcular_imc(peso,alt):
    imc = peso / (alt ** 2)
    return imc

#Programa principal

peso = float(input("Ingrese su peso: "))
alt = float(input("Ingrese su altura en metros: "))
print(f"Su IMC es: {calcular_imc(peso,alt):.2f}")
